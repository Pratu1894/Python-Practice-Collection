a=10
b=10
#if we have same data value , in  heap it stors the data in same location , so both are having smae id
print("id of int")
print(id(a))
print(id(b))
#integer are immutable means we canot change is once declared so it reuse same id for both same values 
print("\n")

print("id of List")
a=[10]
b=[10]
print(id(a))
print(id(b))
#list are mutable means we can change it once it created , so stores in different location even if data value are same



































